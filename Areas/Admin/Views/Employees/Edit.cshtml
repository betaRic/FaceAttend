@model EmployeeEditVm
@{
    ViewBag.Title = "Edit Employee";
}

<div class="admin-card">
    <div class="card-body">
        <h4 class="mb-3">Edit Employee</h4>

        @using (Html.BeginForm())
        {
            @Html.AntiForgeryToken()

            <div class="row g-3">
                <div class="col-md-4">
                    <label class="form-label">EmployeeId</label>
                    @Html.TextBoxFor(m => m.EmployeeId, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.EmployeeId, "", new { @class = "text-danger" })
                </div>

                <div class="col-md-8">
                    <label class="form-label">First Name</label>
                    @Html.TextBoxFor(m => m.FirstName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.FirstName, "", new { @class = "text-danger" })
                </div>

                <div class="col-md-6">
                    <label class="form-label">Middle Name</label>
                    @Html.TextBoxFor(m => m.MiddleName, new { @class = "form-control" })
                </div>

                <div class="col-md-6">
                    <label class="form-label">Last Name</label>
                    @Html.TextBoxFor(m => m.LastName, new { @class = "form-control" })
                    @Html.ValidationMessageFor(m => m.LastName, "", new { @class = "text-danger" })
                </div>

                <div class="col-md-6">
                    <label class="form-label">Position</label>
                    @Html.TextBoxFor(m => m.Position, new { @class = "form-control" })
                </div>

                <div class="col-md-6">
                    <label class="form-label">Department</label>
                    @Html.TextBoxFor(m => m.Department, new { @class = "form-control" })
                </div>

                <div class="col-md-6">
                    <label class="form-label">Assigned Office</label>
                    @Html.DropDownListFor(m => m.OfficeId, (SelectList)ViewBag.Offices, "Select office", new { @class = "form-select" })
                    @Html.ValidationMessageFor(m => m.OfficeId, "", new { @class = "text-danger" })
                </div>

                <div class="col-md-6">
                    <div class="form-check mt-4">
                        @Html.CheckBoxFor(m => m.IsFlexi, new { @class = "form-check-input" })
                        <label class="form-check-label">Flexi schedule</label>
                    </div>
                </div>

                <div class="col-md-6">
                    <div class="form-check mt-4">
                        @Html.CheckBoxFor(m => m.IsActive, new { @class = "form-check-input" })
                        <label class="form-check-label">Active</label>
                    </div>
                </div>
            </div>

            <div class="mt-3 d-flex gap-2">
                <button class="btn btn-primary" type="submit">Save</button>
                <a class="btn btn-outline-primary" href="@Url.Action("Enroll", new { id = Model.Id })">Enroll Face</a>
                <a class="btn btn-outline-secondary" href="@Url.Action("Index")">Back</a>
            </div>
        }
    </div>
</div>

@section scripts{
    @Scripts.Render("~/bundles/jqueryval")
}
