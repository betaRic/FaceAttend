@{
    ViewBag.Title = "Unlock";
}

<div class="container" style="max-width:520px;">
    <div class="admin-card p-3 mt-4">
        <h4 class="mb-2">Admin Unlock</h4>
        <p class="text-muted mb-3">
            Unlock is handled from the Kiosk screen.
        </p>
        <a class="btn btn-primary w-100" href="@Url.Action("Index","Kiosk", new { area = "" })">
            Go to Kiosk
        </a>
        <div class="small text-muted mt-3">
            Press Ctrl + Shift + Space on Kiosk.
        </div>
    </div>
</div>
